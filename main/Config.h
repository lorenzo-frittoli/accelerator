#pragma once // compile only once

// #include <Arduino.h>
#include "RailSwitch.h"

// [RS] RAIL SWITCH
constexpr byte RS_DIR_PIN = 6;
constexpr byte RS_STEP_PIN = 3;
constexpr int RS_NUMBER_OF_STEPS = 110;
constexpr int RS_STEPS_DELTA_TIME = 500;

